# Deployer/configurator key — a THROWAWAY testnet key, funded with Base Sepolia ETH.
# Never a mainnet key.
PRIVATE_KEY=0x...

# Free Etherscan account key (Etherscan v2 covers Base Sepolia) — needed only for
# --verify at deploy time; use the Blockscout verifier if you don't have one.
ETHERSCAN_API_KEY=
