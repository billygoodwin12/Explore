# Address of the deployed VaultFactory on HyperEVM (chain 999).
# Until this is set, the wizard's "Deploy vault" button throws a clear error
# instead of sending a transaction to the zero address.
NEXT_PUBLIC_VAULT_FACTORY_ADDRESS=0x0000000000000000000000000000000000000000
